# Copy to .env.local (never commit real secrets) and fill in values
VITE_APPINSIGHTS_CONNECTION_STRING="InstrumentationKey=YOUR-INSTRUMENTATION-KEY;IngestionEndpoint=https://<region>.in.applicationinsights.azure.com/"

# Optional API base override (no trailing slash). If unset, frontend falls back to current host on port 8000.
# Example: VITE_API_URL="http://localhost:8000"
VITE_API_URL=""
