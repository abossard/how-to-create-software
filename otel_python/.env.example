# Environment variables for docker-compose (copy to .env)
# Build-time (if still used) / legacy Vite variables
VITE_API_URL=http://api:8000

# Runtime-injected API URL used by Fastify to set window.API_BASE.
# Should be accessible from the browser (often localhost mapped to the API container port)
API_URL=http://localhost:8000

OTEL_EXPORTER_OTLP_ENDPOINT=http://aspire:18890
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_SERVICE_NAME_API=api
OTEL_SERVICE_NAME_WORKER=worker
BASE_URL=http://localhost:8000

# Application Insights (optional)
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=...;IngestionEndpoint=...;
